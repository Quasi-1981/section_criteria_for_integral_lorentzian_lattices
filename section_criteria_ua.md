# Section Criteria for Integral Lorentzian Lattices and the Cost of Realization

---

## Анотація

Для цілої ґратки `Λ_W` сигнатури (3,1) і додатно означеної цілої `L` рангу 3 ми даємо **критерій**
того, що `L` є **примітивним часоподібним перерізом** `Λ_W` за примітивним вектором норми `−n`.
Критерій
оперує трьома даними — **сигнатура, білінійна диск-форма і парність** (тип I/II) — і каже: такий
переріз існує **тоді й лише тоді**, коли існує анти-ізометрія підгруп склейки `γ : H_L → H_t`, граф
`Γ` якої задовольняє `Γ^⊥/Γ ≅ disc(Λ_W)` **як ізометрію форм**, а склеєна надґратка має **ту саму
парність**, що `Λ_W`. Обидві вимоги несучі: порядкова версія першої недостатня, а без другої
твердження **хибне** — контрприклад на амбієнті `diag(1,1,2,−2)` наведений явно в §3.4. Критерій
сформульований **відносно фіксованого ambient** і виражений **виключно** в диск-даних та парності —
**Ґрам у нього не входить**. Зворотна імплікація спирається на дві перевірні умови: **(Т0)**
(«названі дані визначають рід ambient») і **(Т0c)** («рід ambient містить один клас»); для
рангу 4, сигнатури (3,1) і `|det| ≤ 4` — скоупу, що покриває всі чотири амбієнти роботи, —
обидві **доведені**: (Т0) повним переліком родів (їх рівно сім, і трійка даних розрізняє їх
попарно, §3.5), (Т0c) — класичною теоремою Айхлера із запасом `5⁶` (§3, Remark 1).

Дві прямі перевірки. **По-перше**, підстановка `disc(Λ_W) = 0` повертає умову `⟨1/n⟩` для
унімодулярного випадку, тобто теорему preprint-6: узагальнення **згортається назад у надруковане**.
**По-друге**, райдер парності показує, що ця межа **точна**: при `p ≡ 1 (mod 8)` умова `⟨1/n⟩` уже
не достатня, бо дві ґратки з **ідентичною** диск-формою лягають у різні ambient, і їх розрізняє
додатковий біт — парність. Випадок `p = 3` при цьому ізольований (`II_{3,1}` не існує), тому
надрукований результат preprint-6 **стоїть як є**.

З критерію випливає, що **мінімальний дискримінант ambient** — «ціна» ґратки `L` — залежить
**лише від білінійної диск-форми** `L`, а отже є **інваріантом її роду**; ми даємо для нього
**замкнений вираз**
`cost(L) = min{ |disc(L)|·n_min(m)/m² : m | exp(disc L) }`.
Скінченність діапазону тут **структурна**, а не обчислювальна, тож вираз не містить межі пошуку —
і саме тому він **стійкий до обрізання**, яке в попередніх, обмежених обчисленнях тричі давало
завищені значення. Обчислення виразу на чотирнадцяти представниках дає таблицю цін; вона
індексована **диск-формою**, а не типом Браве і не порядком диска, бо ціна **не є** інваріантом
типу і **не монотонна** за порядком. Зокрема, центрування кубічної ґратки дістають точні адреси:
**FCC — 3**, **BCC — 8**, а блокування нижчих значень для FCC розпадається на **два різні**
механізми (нерозв'язність `k² ≡ 3 (mod 4)` при `d = 1`; нерозв'язність `2k² ≡ 3 (mod 4)` при
`d = 2`), тоді як `d = 3` не блокується нічим.

Жодного фізичного прочитання в роботі немає: сигнатура (3,1) виступає як арифметична умова —
зокрема як **умова застосовності** класичної теореми про рід невизначених форм, на яку критерій
спирається в одному явно названому кроці.

---

## §1 · Постановка

Preprint-6 (Zenodo `10.5281/zenodo.22068307`) класифікував примітивні часоподібні перерізи
**унімодулярної** ґратки `I_{p,1}` умовою `⟨1/n⟩` (`P6.Thm1(7)`).

**Питання цієї роботи:** що заміняє цю умову для **довільної** цілої лоренцевої ґратки.

**Що саме питається — і в якому кванторі:**
ґратка `Λ_W` вважається **ДАНОЮ**. Уся робота відповідає на питання

> «чи є `L` примітивним часоподібним перерізом **ЦІЄЇ** ґратки `Λ_W`»,

а **не** «чи існує **якийсь** ambient, у який `L` вкладається». Різниця не риторична: у другому
прочитанні відповідь майже завжди «так» і питання порожнє; змістом воно наповнюється **лише** при
фіксованому ambient — і саме цей квантор несе формулювання **P7.Thm1**.
Єдине місце, де ambient **не** фіксований, а **шукається**, — означення **ціни** (P7.Def5): там
береться мінімум по всіх допустимих `Λ_W`, і це сказано в самому означенні.

**Скоуп, сказаний одразу і один раз:** у цій роботі **немає жодного фізичного прочитання**. Жоден
об'єкт не читається як «час», «енергія» чи «маса»; сигнатура (3,1) виступає **виключно** як
арифметична умова — і в одному місці (Remark 1) як **умова застосовності** класичної теореми.

---

## §2 · Означення

**P7.Def1 — диск-форма.** Для невиродженої цілої `L`: `disc(L) := L^∨/L` з **білінійною** формою
`b_L : disc(L) × disc(L) → ℚ/ℤ`. Скорочення `b̄` вживається там, де йдеться про диск-форму як
**інваріант** (без указання ґратки в індексі).

> **Чому `b mod ℤ`, а не `q mod 2ℤ`:** квадратична версія визначена лише для **парних** ґраток, а
> `⟨−n⟩` **непарна при непарному `n`** — і це не крайовий випадок, а основний. Тому весь критерій
> формулюється в білінійній формі. Ціна цього вибору названа явно: білінійна форма **не бачить
> парності**, тому парність входить у дані критерію **окремим бітом** (§3).

**P7.Def2 — підгрупи склейки й анти-ізометрія.** Підгрупи `H_L ⊆ disc(L)`, `H_t ⊆ disc(⟨−n⟩)`
називаємо **підгрупами склейки** (*glue subgroups*). Ізоморфізм `γ : H_L → H_t` — **анти-ізометрія**,
якщо `b_t(γx, γy) = −b_L(x, y)`.

> Термін не винаходиться: «склейка/glue» — усталене слово конструкції (📖 Conway–Sloane, SPLAG
> гл. 4: *glue group*, *glue vectors*; Нікулін §1.5). Ми іменуємо лише **підгрупи**, на яких склейка
> визначена, бо на них посилаються всі наслідки.

**P7.Def3 — граф.** `Γ := {(x, γx) : x ∈ H_L} ⊆ disc(L) ⊕ disc(⟨−n⟩)`. `Γ` **ізотропний**, якщо
`b_L ⊕ b_t` обнуляється на `Γ` — для графа анти-ізометрії це виконано **тотожно**:
`b_L(x,y) + b_t(γx,γy) = b_L(x,y) − b_L(x,y) = 0`.

> **Два боки однієї еквівалентності:** тут — «анти-ізометрія ⟹ ізотропність»; у (Thm1.3) —
> зворотне. Разом: для підгруп з ін'єктивними проєкціями **ізотропні графи є рівно графами
> анти-ізометрій**.

**P7.Def4 — переріз.** Ціла додатно означена `L` рангу 3 (абстрактна) є **примітивним
часоподібним перерізом** `Λ_W` за `t`, якщо `t ∈ Λ_W` **примітивний** (`t/k ∉ Λ_W` для жодного
цілого `k ≥ 2`), `⟨t,t⟩ = −n < 0`, і існує ізометричне вкладення `ι : L ↪ Λ_W` з
`ι(L) = t^⊥ ∩ Λ_W`. Скорочення `L = t^⊥ ∩ Λ_W` далі читається через це `ι` — зворотна нога
доводить **існування вкладення**, не тотожність множин.

> **Примітивність стоїть на `t` — і це несуча вимога; на `L` вона автоматична.**
> Автоматичність: `Λ_W ∩ (L⊗ℚ) = Λ_W ∩ t^⊥ = L` за означенням — перетин ґратки з підпростором
> примітивний завжди. Несучість: без вимоги на `t` теорема §3 **хибна**. Свідок: `Λ_W = I_{3,1}`,
> `t = 2e₄`, `n = 4`, `L = t^⊥ ∩ Λ_W = ℤ³` — переріз існує, але права нога критерію падає:
> `disc(L) = 0` ⟹ єдина анти-ізометрія — порожня, `Γ = 0`, і `Γ^⊥/Γ ≅ ℤ/4 ≇ disc(I_{3,1}) = 0`.
> Той самий `ℤ³` є перерізом за примітивним `e₄` з `n = 1` — норма перерізу коректно означена
> лише на примітивному векторі.

**P7.Def5 — ціна.**
```
   cost(L) := min { |det Λ_W| : Λ_W ціла, сигнатури (3,1),
                    і L — примітивний часоподібний переріз Λ_W за деяким вектором }
```

**P7.Def6 — `j_min` і допустимі порядки склейки.**
```
   D(L)      := { m ≥ 1 : m | exp(disc L) }
   j_min(m)  := min { j ≥ 1 : ∃ x ∈ disc(L) порядку m, ∃ одиниця k mod m,
                              j·k² ≡ b_L(x,x)·m  (mod m) } ,   m ∈ D(L)
   n_min(m)  := m · j_min(m)
```
> **Чому `exp`, а не `|disc|`:** взяти `m | |disc(L)|` означало б допустити `m` без елемента
> порядку `m`, тобто член під `min` був би **невизначеним**. Приклад: `ℤ/2 × ℤ/2` має `4 | 4`, але
> `exp = 2`. Обчисленню це не міняє нічого — **твердженню міняє все**.
> Окремим означенням, бо `j_min` стоїть **у формулюванні** теореми, і на нього посилаються
> наслідки: величина, названа лише всередині теореми, цитується переписуванням.

---

## §3 · P7.Thm1 — критерій склейки

> **P7.Thm1.** Нехай `L` — додатно означена ціла ґратка рангу 3, `n ≥ 1`, і нехай `Λ_W` — ціла
> ґратка сигнатури (3,1) **(обидві фіксовані)**, що задовольняє умову
>
> **(Т0)** трійка даних **(сигнатура, білінійна диск-форма, парність)** визначає рід `Λ_W`
> однозначно серед цілих ґраток — умова **перевірна скінченним порівнянням** (для рангу 4,
> sig (3,1), `|det| ≤ 4` — зокрема для всіх чотирьох амбієнтів роботи — вона **доведена**
> переліком родів, P7.Prop1, §3.5);
>
> **(Т0c)** рід `Λ_W` містить **один клас ізометрії**. (Клас = спінорний рід для невизначених
> рангу ≥ 3 — 📖 Eichler; рід з >1 спінорним родом у ранзі 4 вимагає `|det| ≥ 5⁶ = 15625` —
> 📖 SPLAG Ch. 15, Cor. 22; отже при `|det| ≤ 4` (Т0c) виконана з запасом — Remark 1, нога 1.
> Без (Т0c) зворотна нога дає лише «`Λ` у роді `Λ_W`», не ізометрію.)
>
> Тоді **`L` є примітивним часоподібним перерізом
> `Λ_W` за примітивним вектором норми `−n`** (P7.Def4) ⟺ **існує анти-ізометрія підгруп склейки
> `γ : H_L → H_t`**, граф
> `Γ` якої задовольняє **обидві** умови:
>
> **(Т1)** `Γ^⊥/Γ ≅ disc(Λ_W)` — **як ізометрію форм**, не лише як ізоморфізм груп;
> **(Т2)** склеєна надґратка `Λ(Γ)` має **ту саму парність** (тип I/II), що `Λ_W`; парність
> зчитується з норм глю-векторів `mod 2ℤ`.
>
> (Ізотропність `Γ` — автоматична за P7.Def3.)



**(Т1) «як форму», не «як групу».** Порядкова версія падає на першому нециклічному дискримінанті
(**P7.Cor3**, §6): `ℤ/4` і `ℤ/2×ℤ/2` мають однаковий порядок 4 і не ізометричні. Але й самої
**групи** мало: на `ℤ/6` живуть **дві неізометричні форми**, і вони дають **різні ціни** — `b̄` на
твірному `5/6` дає 2, а `1/6` дає 1 (§7.2 проти рядка *oP* у §7.4). Група — не форма.

**(Т2) парність — третій член даних, і він не декоративний.** Критерій оперує трійкою
**сигнатура + білінійна диск-форма + парність**. З цими даними еквівалентність тримається для
**всіх чотирьох** амбієнтів роботи: на трьох парний конкурент **не існує** (виключення за формулою
Мілґрема — таблиця **§3.4**), на четвертому **біт розрізняє** непарний `diag(1,1,2,−2)` від парного
`A₁²⊕U`. Без `(Т2)` твердження **хибне** саме на четвертому — явна пара в §3.4.

### Remark 1 — на чому теорема стоїть (читати ДО доведення)

Крок потребує, щоб дані критерію визначали `Λ_W` **однозначно**, а не з точністю до роду. Він
стоїть на **двох** ногах, і їх треба бачити нарізно — злитість була причиною, чому друга довго
не перевірялась.

**Нога 1 — «клас = рід» для наших об'єктів.**
Класична теорема (**Eichler**; **Kneser**) для **невизначених** цілих ґраток **рангу ≥ 3**
стверджує: **клас = СПІНОРНИЙ рід**. 📖 поіменний імпорт, у цій роботі **не перевиводиться**.
Наш ambient має ранг **4**, тож умова рангу виконана.
Класична теорема дає «клас = **спінорний рід**», а крок потребує «клас = **рід**». Це **не
тотожність**: рід може містити **кілька** спінорних родів. Для наших об'єктів різниці немає, і це
не оцінка, а рахунок: у ранзі 4 рід із **більш ніж одним** спінорним родом вимагає
`|det| ≥ 5⁶ = 15625` (📖 SPLAG Ch. 15, Cor. 22); наші чотири амбієнти мають `|det| ∈ {1, 2, 4, 4}`
— **запас майже чотиритисячократний**, тож умова виконана **тривіально**, а не «на межі».
Це і є умова **(Т0c)** теореми: у скоупі роботи вона доведена, для довільного `Λ_W` — стоїть у
боксі окремою гіпотезою (рід з кількома класами зробив би висновок (Thm1.11) хибним).

**Нога 2 — парність, і межа того, що вона дає.**
«Сигнатура + **білінійна** диск-форма» **не визначають рід**: парність — інваріант роду, якого
білінійна форма **не бачить**. Контрприклад: `A₁³⊕⟨−2⟩` склеюється різними `Γ` у **парну** `A₁²⊕U`
і **непарну** `diag(1,1,2,−2)` — ті самі сигнатура, диск-група й `b̄`, різні роди. Саме тому
парність стоїть у даних критерію окремою умовою **(Т2)**.

> **Межа ноги 2 — і скоуп, у якому вона закрита.** З того, що парність **необхідна**, не
> випливає, що трійки «сигнатура + `b̄` + парність» **достатньо** для визначення роду **будь-якої**
> ґратки: 2-адичний символ роду непарної ґратки загалом тонший за один глобальний біт (він несе
> тип і oddity кожної жорданової компоненти). Тому достатність стоїть окремою умовою **(Т0)** —
> і для рангу 4, sig (3,1), `|det| ≤ 4` вона **доведена** повним переліком родів (**P7.Prop1**,
> §3.5): родів рівно сім, трійок сім, відповідність один-до-одного. Для довільного `det` (Т0)
> **не заявляється** — загальної характеризації (Т0)-ґраток робота не дає і не потребує.

**Без цих двох ніг** критерій дає лише: `Λ` **того самого роду**, що `Λ_W` — тобто «переріз
**деякої** ґратки роду» замість «переріз **саме** `Λ_W`». Уся сила «⟺» тримається на цьому кроці.

**Контрфакт — чому сигнатура (3,1) є умовою застосовності.** Для **означених** ґраток клас і рід
**можуть розходитись**, і аргумент (Thm1.11) там **недоступний**; отже для означеного ambient
критерій у наведеній формі не був би обґрунтований. Сигнатура (3,1) — **умова застосовності**, не
зручність запису.

### P7.Lem1 — тотожність склейки (білінійна, без обмеження парності)

> **P7.Lem1.** Нехай `M` — невироджена ціла ґратка, `Λ ⊇ M` — ціла надґратка скінченного індексу,
> `Γ := Λ/M ⊆ M^∨/M = disc(M)`, і `Γ^⊥ := {x ∈ disc(M) : b̄(x, Γ) = 0 у ℚ/ℤ}`. Тоді
> `disc(Λ) ≅ Γ^⊥/Γ` — як групи з білінійною формою `mod ℤ`.

*Доведення.* `M ⊆ Λ` (дано); цілість `Λ` дає `Λ ⊆ Λ^∨`; з `M ⊆ Λ` маємо `Λ^∨ ⊆ M^∨` — разом
ланцюг `M ⊆ Λ ⊆ Λ^∨ ⊆ M^∨`. Для `x ∈ M^∨`: `b(x, Λ) ⊆ ℤ` рівносильно `b(x, γ) ∈ ℤ` для всіх
`γ ∈ Γ` (на `M` цілість автоматична), тобто `(x mod M) ∈ Γ^⊥`; отже `Λ^∨/M = Γ^⊥`. Оскільки
`Λ/M = Γ`, третя теорема про ізоморфізм дає `disc(Λ) = Λ^∨/Λ = (Λ^∨/M)/(Λ/M) = Γ^⊥/Γ`, і
білінійна форма на `Λ^∨/Λ` індукується з форми `M` тотожно. ∎

> **Навіщо ця лема тут, а не цитата.** Класичний виклад цієї тотожності (Нікулін) ведеться через
> **квадратичну** диск-форму `q mod 2ℤ`, означену лише для **парних** ґраток — а наш випадок
> непарний. Груповий і білінійний бік тотожності, як показує доведення, **парності не потребує
> взагалі** — тож для непарних ґраток вона доводиться, а не цитується. Числова звірка обох сторін
> (незалежними обчисленнями) виконана на трьох непарних склейках і одній парній контрольній.

### P7.Lem2 — склейка за ізотропною підгрупою

> **P7.Lem2.** Нехай `M` — невироджена ціла ґратка, `Γ ⊆ disc(M)` — підгрупа, і
> `Λ_Γ := {x ∈ M^∨ : x mod M ∈ Γ}`. Тоді:
> **(а)** `Λ_Γ` — підгрупа `M^∨` з `M ⊆ Λ_Γ` і `Λ_Γ/M = Γ`; **`Λ_Γ` ціла ⟺ `Γ` ізотропна**;
> **(б)** для `M = L ⊕ ⟨t⟩`: `L` примітивна в `Λ_Γ` ⟺ проєкція `Γ → disc(⟨t⟩)` **ін'єктивна**;
> симетрично, `⟨t⟩` примітивний у `Λ_Γ` ⟺ проєкція `Γ → disc(L)` **ін'єктивна**.

*Доведення.* **(а)** `Λ_Γ` — прообраз підгрупи `Γ` при `M^∨ → disc(M)`, отже підгрупа; `M` —
прообраз нуля, тож `M ⊆ Λ_Γ` і `Λ_Γ/M = Γ`. Цілість: для `x, y ∈ Λ_Γ` значення `b(x, y) mod ℤ`
дорівнює `b̄(x̄, ȳ)`, отже `b(Λ_Γ, Λ_Γ) ⊆ ℤ` ⟺ `b̄` обнуляється на `Γ` (пари з `x̄ = 0` цілі
автоматично: `x ∈ M`, `y ∈ M^∨`). **(б)** `Λ_Γ ∩ (L⊗ℚ) ⊆ M^∨ ∩ (L⊗ℚ) = L^∨`, тож елемент
перетину має образ вигляду `(x̄, 0) ∈ Γ`; отже `Λ_Γ ∩ (L⊗ℚ) = L` ⟺ `Γ` не містить ненульових
елементів з нульовою `⟨t⟩`-компонентою ⟺ `Γ → disc(⟨t⟩)` ін'єктивна. Симетрично для `⟨t⟩`. ∎

> Лема закриває одним місцем чотири кроки, що на неї спираються: (Thm1.2)/(Thm1.3) у прямій нозі
> (там `Λ_W = Λ_Γ` для `Γ = Λ_W/M`), (Thm1.7)/(Thm1.10) у зворотній, і ланцюг (Thm2.9).

### §3.1 · Доведення: пряма нога (переріз ⟹ γ)

**(Thm1.1)** `t` примітивний, `⟨t,t⟩ = −n`, `L = t^⊥ ∩ Λ_W` (примітивна автоматично, P7.Def4)
⟹ `L ⊕ ⟨t⟩ ⊆ Λ_W` — підґратка скінченного індексу.
**(Thm1.2)** `Γ := Λ_W/(L ⊕ ⟨t⟩)`; цілість `Λ_W` ⟹ `Γ` **ізотропний** *(P7.Lem2(а), напрям
«ціла ⟹ ізотропна»; для графа анти-ізометрії це й тотожно за P7.Def3)*.
**(Thm1.3)** Примітивність `L` (автоматична) дає ін'єктивність `Γ → disc(⟨t⟩)`; примітивність
`⟨t⟩` (вимога P7.Def4 — рівно тут вона й несе) дає ін'єктивність `Γ → disc(L)` *(обидва —
P7.Lem2(б))* ⟹
`Γ` є графом ізоморфізму `γ`. Анти-ізометрію читаємо **білінійно**, з ізотропності на парі різних
елементів `(x,γx)`, `(y,γy) ∈ Γ`:
```
   0 = (b_L ⊕ b_t)((x,γx),(y,γy)) = b_L(x,y) + b_t(γx,γy)   ⟹   b_t(γx,γy) = −b_L(x,y)
```
для **всіх** `x, y` — саме те, що вимагає P7.Def2. *(Через діагональ `b(x,x)` цього виводити не
можна: над `ℚ/ℤ` поляризація не відновлює білінійну форму на 2-крученні.)*
**(Thm1.4)** `disc(Λ_W) ≅ Γ^⊥/Γ` як форма — умова **(Т1)**. *(P7.Lem1.)*
**(Thm1.4′)** Норми глю-векторів `Λ_W/(L⊕⟨t⟩)` `mod 2ℤ` дають парність `Λ_W` — умова **(Т2)**
виконана тотожно, бо надґраткою і є сама `Λ_W`.
**(Thm1.5)** `disc(⟨t⟩) ≅ ℤ/n` з `b(k,k) = −k²/n`. ∎

### §3.2 · Доведення: зворотна нога (γ ⟹ Λ_W)

**(Thm1.6)** `M := L ⊕ ⟨−n⟩`, сигнатура `(3,0)+(0,1) = (3,1)`.
**(Thm1.7)** Ізотропна `Γ ⊆ disc(M)` визначає **цілу** надґратку `Λ := Λ_Γ ⊇ M`, `Λ/M ≅ Γ`
*(P7.Lem2(а))*.
**(Thm1.8)** Сигнатура зберігається ⟹ `Λ` має (3,1).
**(Thm1.9)** `disc(Λ) ≅ Γ^⊥/Γ` *(P7.Lem1 — та сама лема, що в (Thm1.4))*, а
`Γ^⊥/Γ ≅ disc(Λ_W)` — за умовою **(Т1)**.
**(Thm1.10)** Примітивність — **дві різні властивості, кожна зі своєю підставою** *(P7.Lem2(б))*:
`L` примітивна ⟸ ін'єктивність `Γ → disc(⟨t⟩)`; `⟨t⟩` примітивна ⟸ ін'єктивність `Γ → disc(L)`;
обидві ін'єктивності автоматичні, бо `Γ` — графік **ізоморфізму** `γ`.
**(Thm1.11)** `Λ` і `Λ_W` мають однакові сигнатуру, диск-форму **і парність** (остання — за умовою
**(Т2)**). За умовою **(Т0)** ці дані визначають рід `Λ_W` однозначно ⟹ `Λ` і `Λ_W` в одному
роді; за умовою **(Т0c)** цей рід містить один клас ⟹ `Λ ≅ Λ_W`. Для амбієнтів цієї роботи
обидві умови доведені: (Т0) — переліком родів (P7.Prop1, §3.5), (Т0c) — 📖 **Eichler** + запас
`5⁶` (Remark 1, нога 1). ∎

### §3.3 · Вимір, що супроводжує доведення

Два **незалежні шляхи** до одного числа: **шлях (1)** — прямий перебір примітивних `t`,
`t² = −n ≤ 4`; **шлях (2)** — формула склею `|disc(L)|·n = |disc(Λ_W)|·index²`.

| ambient | `\|disc\|` | що дав перебір | (1) проти (2) |
|:--|:--|:--|:--|
| `I_{3,1}` **(контроль, disc = 0)** | 1 | `n → \|disc(L)\| = n`, циклічна `ℤ/n` | **100 %** |
| `diag(1,1,1,−2)` | 2 | `1→2`, `2→{1, ℤ/2×ℤ/2}`, `3→6`, `4→ℤ/2×ℤ/4` | **100 %** |
| `diag(1,1,1,−4)` | 4 | `1→4`, `2→ℤ/2×ℤ/4`, `3→12`, `4→1` | **100 %** |
| `A₃⊕⟨−1⟩` — **той самий ambient**, що рядок вище | 4 | `1→4` (`= A₃`), `2→ℤ/2×ℤ/4`, `3→12` | **100 %** |

Останні два рядки описують **одну** ґратку: `diag(1,1,1,−4) ≅ A₃ ⊕ ⟨−1⟩` — обидві мають вектор
норми `−1`, комплемент у обох — `A₃`, θ-ряди тотожні. Ідентичність їхніх стовпців — не збіг, а
наслідок цієї ізометрії.
⟹ **вимір покрив три різні амбієнти з чотирьох.** Четвертий, `diag(1,1,2,−2)`, входить у роботу
свідком §6 і §3.4; тест «(1) проти (2)» і не міг би на ньому нічого зловити — обидва шляхи перелічують
**реальні** перерізи, тож хибна зворотна нога на них не видна. Саме тому знадобився
контрприклад (§3.4), а не ширший перебір.

Унімодулярний рядок — **контроль, не ілюстрація**: перебір **сам** відтворив `⟨1/n⟩`, не будучи на
нього налаштованим. Це **§4 у дії**.

### §3.4 · Коли (Т2) виконується сама: виключення парного конкурента

Умова **(Т2)** нетривіальна лише там, де **парний конкурент існує** — тобто де є парна ґратка з
тією самою сигнатурою й тією самою `b̄`, що `Λ_W`. Це питання закривається **необхідною** умовою
Мілґрема, і відповідь для чотирьох амбієнтів роботи вміщується в таблицю.

**Механізм.** Парна ґратка несе квадратичне уточнення `q : disc → ℚ/2ℤ` з `b_q = b̄`. Формула
Мілґрема (📖 §10) пов'язує сигнатуру ґратки з гаусовою сумою уточнення:
```
        GS(q) = Σ_x e^{πi·q(x)} = √|disc| · e^{πi·σ(q)/4} ,      σ(Λ) ≡ σ(q)  (mod 8)
```
Сигнатура (3,1) дає `σ = 2`. Отже: якщо **жодне** уточнення `q` форми `b̄` не має `σ(q) ≡ 2 (mod 8)`,
то парної ґратки з цими даними **не існує** — конкурента немає, і (Т2) виконується автоматично.
Уточнень скінченно (на циклічному твірному порядку `m` величина `q(g)` визначена `b̄(g,g)` з
точністю до `ℤ/2ℤ`), тож перебір **повний**, а не вибірковий.

| ambient | `disc` | `σ(q)` — усі досяжні | парний конкурент |
|:--|:--|:--|:--|
| `I_{3,1}` | тривіальна | `{0}` | **виключено** |
| `diag(1,1,1,−2)` | `ℤ/2` | `{1, 7}` | **виключено** |
| `diag(1,1,1,−4)` | `ℤ/4` | `{3, 7}` | **виключено** |
| `diag(1,1,2,−2)` | `ℤ/2 × ℤ/2` | `{0, **2**, 6}` | **НЕ виключено** |

**Три висновки.**

**(i)** На трьох перших `2 ∉ σ(q)` ⟹ парного конкурента немає ⟹ **(Т2) виконується сама**, і
критерій там працює на самих лише сигнатурі й `b̄`.
**(ii)** Перший рядок — це **Remark 2** (§5) у ширшій нотації: при тривіальному диску єдине
уточнення `q = 0` дає `σ(q) = 0`, тобто «парна унімодулярна сигнатури (3,1) не існує». Два
твердження, які виглядали різними, — **один механізм** `σ mod 8`.
**(iii)** На четвертому `σ(q) = 2` **досяжна** — і конкурент там справді є: `A₁²⊕U`, побудований
явно (§3.4). Тобто таблиця **не перетискає**: вона не виключає того, що існує. Саме на цьому
рядку (Т2) і несе вагу.

**Явна пара четвертого рядка.** `L = A₁³` (Ґрам `diag(2,2,2)`), `n = 2`, `Λ_W = diag(1,1,2,−2)`:
умова **(Т1)** виконана — `H_L = ⟨g₃⟩`, `γ : g₃ ↦ 1` анти-ізометрія (`½ = −½ mod ℤ`), і
`Γ^⊥/Γ ≅ (ℤ/2)²` з `b = diag(½,½)`, що **ізометрично** `disc(diag(1,1,2,−2))`. Але склейка дає
`Λ = A₁²⊕U` — глю-вектор `w = (e₃+t)/2` має `w² = 0`, пара `w, (e₃−t)/2` гіперболічна ⟹ `Λ`
**парна**, тоді як `Λ_W` **непарна**. Умова **(Т2)** відсіює саме цей випадок.

> **Про статус цієї таблиці.** Мілґрем дає умову **необхідну**: `2 ∉ σ(q)` доводить
> **неіснування** конкурента строго. Зворотний бік — що при `σ(q) = 2` конкурент таки існує — з
> Мілґрема **не випливає**, і для четвертого рядка він доведений **не** гаусовою сумою, а **явною
> побудовою** `A₁²⊕U`. Два різні методи, і кожен ужито там, де він щось доводить.

### §3.5 · P7.Prop1 — умова (Т0) доведена переліком родів

> **P7.Prop1.** Цілих ґраток рангу 4, сигнатури (3,1), `|det| ∈ {1, 2, 4}` існує з точністю до
> роду **рівно сім**, і трійка **(диск-група, `b̄`, парність)** набуває на них **семи різних
> значень**. Отже трійка визначає рід: умова **(Т0)** виконана для **кожної** ґратки цього
> скоупу — зокрема для всіх чотирьох амбієнтів роботи.

*Доведення.* Рід невизначеної ґратки визначається сигнатурою і p-адичними символами; для
`|det| ∈ {1,2,4}` нетривіальним є лише 2-адичний. Перелік ведеться згори — з умов існування —
і замикається знизу — явними ґратками.

**(1) Перелік повний.** Умови існування системи p-адичних символів (📖 SPLAG Ch. 15 §7.7:
детермінантна умова, oddity-формула, умови на кожен жордановий складник; Theorem 11 — така
система реалізується цілою формою) дають для сигнатури (3,1) і `|det| ∈ {1,2,4}` **17 путативних
систем**. Склейка еквівалентних систем — *oddity fusion* і *sign walking* — виконана **за
Allcock–Gal–Mark** (📖: Lemma 5.2 — злиття лише коли образ є легальним символом; Lemma 6.1 —
три дозволені випадки прогулянки знаків; **Theorem 6.2 — два символи ізометричні ⟺ пов'язані
ланцюгом цих операцій**), тож лічба точна **в обидва боки**: операції не склеюють
нееквівалентного і не лишають несклеєного:

| `\|det\|` | путативних систем | канонічних родів |
|:--|--:|--:|
| 1 | 1 | **1** |
| 2 | 4 | **1** |
| 4 | 12 | **5** |
| **разом** | **17** | **7** |

Без канонізації лічба хибна: `|det| = 2` дав би «чотири роди» замість одного.

> **Чому склейка ведеться за AGM, а не за «канонічною формою» SPLAG §7.6.** Твердження §7.6
> про єдиність канонічного символу («щонайбільше один мінус на трейн») у загальному випадку
> **хибне** — помилку назвали Allcock–Gal–Mark (§6, слідом за Allcock, Memoirs AMS 220);
> виправлення — *signways*. Тому крок (1) спирається не на §7.6, а на операції AGM з
> Theorem 6.2, і коректність не тримається на відсутності відомої поганої конфігурації: **всі
> 17 систем склеєно виключно операціями, дозволеними Lemma 5.2 і Lemma 6.1** — жодного
> недозволеного sign walk у ланцюгах немає, — а Theorem 6.2 ототожнює класи цієї еквівалентності
> з класами ізометрії, і їх виходить рівно `1 + 1 + 5 = 7`. Виміряний контроль: безумовний
> трейн-волк (без умов Lemma 6.1) дає **вісім** «родів» замість семи — число чутливе до правила,
> тобто збіг із кроком (3) не є тавтологією інструмента.

**(2) Сім родів поіменно.**

| `\|det\|` | канонічний 2-адичний символ | диск-група | парність | представник |
|:--|:--|:--|:--|:--|
| 1 | `1^{+4}_I (t=2)` | тривіальна | непарна | `I_{3,1}` |
| 2 | `1^{+3}_I (t=1) · 2^{+1}_I (t=1)` | `ℤ/2` | непарна | `diag(1,1,1,−2)` |
| 4 | `1^{+3}_I (t=1) · 4^{+1}_I (t=1)` | `ℤ/4` | непарна | `diag(1,1,4,−1)`, `b̄ = ¼` |
| 4 | `1^{+3}_I (t=3) · 4^{+1}_I (t=7)` | `ℤ/4` | непарна | `diag(1,1,1,−4) ≅ A₃⊕⟨−1⟩`, `b̄ = ¾` |
| 4 | `1^{+2}_I (t=0) · 2^{+2}_I (t=2)` | `ℤ/2×ℤ/2` | **непарна** | `diag(1,1,2,−2)` |
| 4 | `1^{+2}_I (t=2) · 2^{+2}_{II} (t=0)` | `ℤ/2×ℤ/2` | непарна | `I₂⊕U(2)`, `b̄` з нульовою діагоналлю |
| 4 | `1^{+2}_{II} (t=0) · 2^{+2}_I (t=2)` | `ℤ/2×ℤ/2` | **парна** | `A₁²⊕U` |

> **Омонім названо:** `t` **усередині символів** — oddity жорданового складника (нотація SPLAG
> §7.4); з часоподібним вектором `t` критерію (P7.Def4) спільного нічого.

Представники рядків 3 і 6 — явні: `diag(1,1,4,−1)` має диск `ℤ/4` з `b̄(g,g) = (¼)²·4 = ¼`;
`I₂⊕U(2)` (`U(2)` — Ґрам `[[0,2],[2,0]]`) має диск `(ℤ/2)²` з нульовою діагоналлю і
`b̄(x,y) = ½`; обидві непарні, сигнатури (3,1), `|det| = 4` — і сама Prop1 пінить кожну в її
рядок: трійка збігається рівно з одним родом. Повний перелік 17 путативних систем і карта
склейки `17 → 7` — supplementary **S1** (`S1_genus_enumeration_17_to_7.md`).

**(3) Сім трійок — і це рахується без символів.** Колонки «диск-група / `b̄` / парність» вище
зчитані з символів і в принципі могли б розрізняти тонше, ніж справжня ізометрія `b̄`, — тоді
«сім різних трійок» було б артефактом запису. Тому розрізнення взяте з **незалежного** перерахунку
знизу: на явних Ґрамах цього скоупу трійка — з `b̄` як справжньою скінченною формою, ізометрія
вичерпним перебором — набуває **семи різних значень**, і кожне реалізоване. Трійка — інваріант
роду; родів рівно сім, значень сім ⟹ відображення «рід → трійка» — **ін'єкція**. ∎

**Дві перевірки таблиці.** Рядки 5 і 7 — пара §3.4 (`diag(1,1,2,−2)` проти `A₁²⊕U`): однакова
диск-група, різна парність — і **два різні** канонічні символи; канонізація не склеює того, що
§3.4 розрізнив явною побудовою. Рядок 1 — єдиний рід при `|det| = 1`, і він **непарний**: парний
варіант відпадає, бо тип II дає oddity `t ≡ 0`, а oddity-формула вимагає `t ≡ 2` — це Remark 2
(§5), перевиведений з умов існування, а не процитований.

**Який інваріант розрізняє кожну пару.** Парні конкуренти трьох перших амбієнтів виключені за
`σ(q) mod 8` (§3.4); при `|det| = 4` групи `ℤ/4` і `ℤ/2×ℤ/2` розводяться структурою групи; дві
форми на `ℤ/4` — діагоналлю `b̄` (`[0,¼,¼]` проти `[0,¾,¾]`); дві непарні форми на `ℤ/2×ℤ/2` —
лічбою `b`-ізотропних (1 проти 3 ненульових); пара рядків 5/7 — бітом парності **(Т2)**, і це
єдине місце, де він несе вагу.

---

## §4 · P7.Cor1 — повернення до preprint-6

> **P7.Cor1.** `disc(Λ_W) = 0` ⟺ `Γ` лагранжевий ⟺ `disc(L) ≅ ℤ/n` з `b̄_L = ⟨1/n⟩` — тобто
> **P6.Thm1(7)**. Умова `⟨1/n⟩` тут і всюди читається **білінійно** (P7.Def1), у записі P6:
> `b_L(g,g) ≡ a/n (mod ℤ)` на твірному, `a` — квадратичний лишок mod `n`.

**(Cor1.1)** `disc(Λ_W) ≅ Γ^⊥/Γ` ⟹ `disc(Λ_W) = 0 ⟺ Γ^⊥ = Γ` (лагранжевий).
**(Cor1.2)** Рахунок порядків:
```
   |Γ| = |H_L| = |H_t|                                   (i)
   |Γ|² = |disc(L)| · n                                  (ii)
   |H_L| ≤ |disc(L)| ,  |H_t| ≤ n                        (iii)
   ⟹  H_L = disc(L),  H_t = disc(⟨−n⟩),  |disc(L)| = n   (iv)
```
рівність добутків при обмеженнях зверху **змушує рівність кожного співмножника**.
> З (iv) випливає не лише рівність порядків, а що `disc(L)` **ізометрична** циклічній формі — саме
> тут вимога «як форму» починає працювати (необхідність — **§6**).

**(Cor1.3)** З (Cor1.2) `H_L = disc(L)`, тож `γ` — анти-ізометрія **всієї** `disc(L)` на `ℤ/n`:
`b_L(x, y) = −b_t(γx, γy)`. На твірному `g` маємо `γ(g) = k·g_t` з `k` — одиницею mod `n` (`γ` —
ізоморфізм), і за (Thm1.5) `b_t(g_t, g_t) = −1/n`, отже
```
   b_L(g, g) = −k²·b_t(g_t, g_t) = −k²·(−1/n) = k²/n   (mod ℤ)
```
— тобто `b̄_L = ⟨1/n⟩` у білінійному записі (`a = k²` — квадратичний лишок). Квадратична
`q mod 2ℤ` тут не виникає ніде — перенесення суто білінійне. ∎

**Головний аргумент роботи:** preprint-6 **не обходиться і не виправляється** — він **виходить** із
P7.Thm1 підстановкою `disc = 0`. Це **узагальнення зі згортанням назад у надруковане**.

---

## §5 · P7.Cor2 — райдер парності: межа preprint-6 точна

> **P7.Cor2.** Якщо склеєна надґратка `Λ(Γ)` **парна**, то `L` і `⟨−n⟩` **обидві** парні.
> **Наслідок:** при `p ≡ 1 (mod 8)` умова `⟨1/n⟩` **не достатня** — вона не розрізняє парний
> ambient від непарного.

*Доведення необхідності.* `M = L ⊕ ⟨−n⟩ ⊆ Λ(Γ)`, тож кожен вектор `M` має парну
норму. ∎

> **Зворотне НЕ чинне:** парність надґратки читається з норм
> **глю-векторів** `mod 2ℤ`, а не з парності доданків. Свідок — власна пара §3.4: **парна**
> `M = A₁³⊕⟨−2⟩` склеюється за одним класом у **парну** `A₁²⊕U`, а за іншим у **непарну**
> `diag(1,1,2,−2)`. Саме тому парність увійшла в дані критерію окремою умовою **(Т2)**, а не
> як наслідок парності доданків.

| ґратка | **диск-форма** | **парність** | ambient |
|:--|:--|:--|:--|
| `E₈ ⊕ ⟨2⟩` | `ℤ/2`, `b = ½` | **парна** | лише `II_{9,1}` |
| `I₈ ⊕ ⟨2⟩` | `ℤ/2`, `b = ½` — **та сама** | **непарна** | `I_{9,1}` |

⟹ диск-форма ambient **не визначає**; потрібен додатковий **біт**. Отже межа `P6.Thm1(7)`
**ТОЧНА**, а не консервативна.

### Remark 2 — випадок `p = 3`

> **`II_{3,1}` не існує**, тому при `p = 3` парна гілка порожня, райдер спрацювати не може, і умова
> `⟨1/n⟩` **достатня**.

*Доведення.* парна унімодулярна невизначена існує ⟺
`sig ≡ 0 (mod 8)`; для (3,1) `sig = 2 ≢ 0`. ∎
📖 **класифікація невизначених парних унімодулярних** — поіменна нога цього зауваження.


**Позитивно і точно:** межа умови `⟨1/n⟩` **не торкається** випадку `p = 3`, отже надрукований
результат preprint-6 **стоїть як є**; ця робота лише окреслила його scope ззовні.

---

## §6 · P7.Cor3 — форма необхідна

> **P7.Cor3.** Порядкова версія критерію (`|disc(L)|·n = |disc(Λ_W)|·index²`) **недостатня**.

Ambient — наводиться явно, він `4×4` діагональний **і сам є свідком**:
```
        Λ_W = diag(1, 1, 2, −2) ,   |disc| = 4 ,   сигнатура (3,1)
        disc(Λ_W) = ℤ/2 × ℤ/2 ,   форма b = {0, 0, ½, ½}
```

| `t²` | `\|disc(L)\|` | `index²` | арифметика склею |
|:--|:--|:--|:--|
| −1 | 4 | 1 | ✓ |
| −2 | 2 | 1 | ✓ |
| −3 | 12 | 9 | ✓ |
| −4 | 16 | 16 | ✓ |

**Арифметика склею проходить на ВСІХ чотирьох — і саме тому нічого не вирішує.** Розрізняє форма:
`ℤ/4` і `ℤ/2×ℤ/2` мають однаковий порядок 4, але **не ізометричні**; `disc(Λ_W)` збігається саме з
`ℤ/2×ℤ/2`.

**Ціна пропуску:** без слів «як форму» **P7.Thm1 хибна** — вона стає твердженням про порядок і
падає на першому нециклічному дискримінанті. Наслідок, який рятує теорему від хибності, мусить це
про себе сказати.

---

## §7 · P7.Thm2 — ціна: закон і замкнена формула

### §7.1 · Закон

> ### **P7.Thm2(a) — ціна залежить ЛИШЕ від білінійної диск-форми `L`.**
> Якщо `b̄_{L₁} ≅ b̄_{L₂}` (при однакових ранзі й сигнатурі), то `cost(L₁) = cost(L₂)`.
> **Наслідок (цитовна форма):** ціна є **інваріантом роду** `L` — рід визначає диск-форму.
> Інваріантність за родом **строго слабша**: свідок §7.2(i) має однакову ціну при **різних** родах.

> **Порядок доведення: (a) виводиться з (b), а не з Thm1.** Це не стилістика. Після ремонту
> дані Thm1 містять **парність (Т2)**, тож фразу «критерій виражений виключно в диск-даних» уже
> **не можна** брати за посилку — вона перестала бути точною. Натомість **замкнена формула §7.3
> демонстровано їсть лише `b̄`**: ані парність, ані Ґрам у неї не входять. Тому (a) доводиться
> **після** (b) і **з** (b) — конструктивно, а не риторично.

**(Thm2.1)** Формула P7.Thm2(b) виражена через `|disc(L)|`, `exp(disc L)` і значення `b_L` на
елементах `disc(L)` — тобто **виключно** через `b̄` як форму; ані Ґрам, ані парність у неї не
входять. Ізометричні `b̄` дають однакове значення формули, а формула дорівнює ціні ⟹ `cost` є
функцією `b̄`. ∎
**(Thm2.2)** Рід визначає сигнатуру і диск-форму ⟹ на роді ціна стала — **інваріант роду**. ∎

> **Чому тут немає імпорту.** Раніше цей крок посилався на «сигнатура + диск-форма **і є** рід»
> (📖 Нікулін). Це посилання і **зайве**, і **завузьке**: потрібен лише тривіальний напрямок
> (рід ⟹ диск-форма), а зворотний для **непарних** ґраток із **білінійною** формою **хибний** —
> власний свідок §5 (`E₈⊕⟨2⟩` і `I₈⊕⟨2⟩`: та сама білінійна диск-форма, різні роди).
> Теорема Нікуліна сформульована для **парних** ґраток і **квадратичної** форми; ми не тягнемо її
> поза скоуп (§10).

> **Чому парність (Т2) ціни не зачіпає — і це видно з означення, а не з надії.** `P7.Def5`
> мінімізує по **всіх** амбієнтах, а не по амбієнтах приписаної парності: кожна допустима склейка
> дає **якусь** цілу надґратку (3,1), і вона й є свідком. Тому додана умова (Т2), яка розрізняє
> амбієнти між собою, мінімум **не зсуває**. Свідок §7.2(i) показує це прямо: пара різниться саме
> парністю і має ціну `1` обоє.
> ⟹ **цінова половина роботи (§7–§8) від ремонту Thm1 не рухається**, і імпортів у неї нуль:
> ні Айхлера, ні роду.

### §7.2 · Два різні твердження — два різні свідки

**(i) Ціна визначається диск-формою — і НЕ бачить ані класу, ані роду.**
Свідок: **однакова диск-форма, однакова ціна, різні ґратки — аж до різних родів.**
Тут Ґрами обов'язкові (`3×3`, одна пара) — суперечка саме **про представника**:
```
   diag(1,1,6)   [[1,0,0],[0,1,0],[0,0,6]]    |Aut| 16   НЕПАРНА   disc ℤ/6, b̄(g,g) = 1/6   ціна 1
   A₂ ⊕ ⟨2⟩      [[2,−1,0],[−1,2,0],[0,0,2]]  |Aut| 24   ПАРНА     disc ℤ/6, b̄(g,g) = 1/6   ціна 1
```
`|Aut|` 16 ≠ 24 ⟹ **не ізометричні**. Більше того: `diag(1,1,6)` **непарна** (`e₁² = 1`), а
`A₂⊕⟨2⟩` **парна** (вся діагональ Ґрама парна), і парність — інваріант роду ⟹ ці дві ґратки лежать
у **різних родах**. Та сама диск-форма ⟹ **та сама ціна** попри це.

> **Свідок доводить більше, ніж «інваріант роду».** Слабша версія («один рід, два класи») була б
> окремим випадком цієї. Змінна, від якої ціна залежить, — **диск-форма**; рід був би зайвим
> посередником, який до того ж тут не збігається.

**(ii) Ціна НЕ є інваріантом ТИПУ Браве** — свідок **протилежний за формою**: той самий **тип**,
**різні** ціни — `A₂⊕⟨2⟩` (гексагональна) → 1 проти рядка `hP` (гексагональна) → 3.

> **Чому (i) не доводить (ii):** однакова ціна у двох ґраток каже, що ціна не бачить **класу** й
> **роду**, і нічого не каже про **тип**. Спростувати інваріантність типу може лише розходження
> **всередині** типу.

### §7.3 · Замкнена формула

> **P7.Thm2(b).** `cost(L) = min { |disc(L)| · n_min(m)/m² : m ∈ D(L) }`, з `D(L)`, `n_min` за
> P7.Def6.

**Крок 1 — СКІНЧЕННІСТЬ** *(окремим кроком і першим: саме він робить вираз теоремою, а не пошуком)*

**(Thm2.3)** `Γ` проєктується в `disc(L)` ін'єктивно (Thm1.3) ⟹ **`Γ` ізоморфна підгрупі `disc(L)`**.
**(Thm2.4)** ⟹ `m := |Γ|` ділить `|disc(L)|` (Лагранж), і **точніше** `m | exp(disc L)`, бо `H_L`
циклічна — це (Thm2.7) нижче, і воно доводиться **незалежно** від цього кроку, тож циркулярності
немає. Дільників скінченно **завжди**.
**(Thm2.5)** Множина в означенні `j_min(m)` **непорожня**, і `j_min(m) ≤ m`. Справді: `m ∈ D(L)`
гарантує існування `x` порядку `m`; при `k = 1` умова читається як `j ≡ A (mod m)`, де
`A := b_L(x,x)·m mod m` — ціле, бо `ord(x) = m`. Клас `j mod m` має представника в `{1, …, m}`
(при `A = 0` це `j = m`) ⟹ множина непорожня і мінімум `≤ m`. Скінченно.
**(Thm2.6)** Допустимих пар `(m, n)` **нескінченно багато**: при фіксованому `m` конгруенція
P7.Def6 допускає **весь клас лишків** `j mod m`, а з ним довільно великі `n = m·j`. Скінченність
живе не в парах, а в мінімізації: цільова величина `|disc(L)|·n/m² = |disc(L)|·j/m` **строго
зростає за `j`** при фіксованому `m`, тож мінімум по класу досягається на `j_min(m)` (непорожньо
і `≤ m` — Thm2.5), а `m` пробігає скінченну `D(L)` (Thm2.4) ⟹ мінімізація зводиться до
скінченної множини `{(m, m·j_min(m)) : m ∈ D(L)}`; мінімум існує, досягається, **без будь-якої
межі пошуку `D_max`**. ∎

> **Формула truncation-стійка за побудовою** — не тому, що межа пошуку `D_max` узята достатньо
> великою, а тому, що межі в ній **немає взагалі** (`D(L)` з P7.Def6 — множина дільників
> `exp(disc L)`, структурний об'єкт, не параметр обрізання).

**Крок 2 — вираз рахує саме те, що треба**

**(Thm2.7)** **Циклічність — наслідок, а не зручний випадок:** `H_t ⊆ ℤ/n` циклічна **завжди**;
`γ` ізоморфізм ⟹ `H_L` циклічна **примусово**. Отже один `m` і один твірний достатні **за
структурою**.
> **Знімає уявну суперечність із §6:** `disc(L)` **може** бути нециклічною (саме про це §6);
> циклічною є **підгрупа склейки**, не диск.

**(Thm2.7′) — скалярна конгруенція Def6 еквівалентна ПОВНІЙ анти-ізометрії.** Окремо, бо `Def6` оперує **одним числом** `b_L(x,x)`, а `Def2` вимагає рівності форм на **всіх**
парах. Для циклічних `H_L = ⟨x⟩`, `H_t = ⟨y⟩` і `γ(x) = k·y` білінійність дає
`b(ax, bx) = ab·b(x,x)`, тож
```
   b_t(γ(ax), γ(bx)) = ab·k²·b_t(y,y)   і   −b_L(ax, bx) = −ab·b_L(x,x)
```
для всіх `a, b` ⟺ `k²·b_t(y,y) = −b_L(x,x)` — **одна** рівність на твірних. Значення форми на
твірному визначає її на всій циклічній підгрупі; тому скалярна умова `Def6` не слабша за `Def2`.
(Циклічність — не припущення, а (Thm2.7).)

Далі — єдине місце справжньої арифметики: `y = (n/m)·g` дає `b_t(y,y) = −n/m²`, з `n = m·j` і
множенням на `m`:
```
        j · k² ≡ b_L(x,x) · m   (mod m)
```
отже `j` пробігає **квадратичні зсуви за одиницями `k`**, і `j_min` є питанням про **квадратичні
лишки mod `m`**.

**(Thm2.8)** Арифметика склею дає `|det Λ_W| = |disc L|·n/m²`; мінімізація по `(m,n)` — це і є вираз.

**(Thm2.9) — кандидат `Def6` дає саме ДОПУСТИМИЙ за `Def5` ambient.** Крок виписується повністю,
бо `Def5` мінімізує **не** по цілих надґратках взагалі, а по тих, де `L` — **примітивний
часоподібний переріз**:

```
   допустима склейка (m, j, k)
   ⟹ Γ = graph(γ), γ : H_L → H_t ізоморфізм            (Thm2.7′)
   ⟹ обидві проєкції Γ → disc(L), Γ → disc(⟨−n⟩)
      ін'єктивні — автоматично, бо графік ІЗОМОРФІЗМУ
   ⟹ ⟨t⟩ примітивна, L примітивна                       (Thm1.10)
   ⟹ L = t^⊥ ∩ Λ : примітивність L дає L = (L⊗ℚ) ∩ Λ,
      а L⊗ℚ = t^⊥⊗ℚ                                     (P7.Def4)
   ⟹ Λ — допустимий ambient за P7.Def5
```
Плюс `Γ` ізотропна за P7.Def3 ⟹ `Λ` **ціла**, сигнатури (3,1) (Thm1.7, Thm1.8). ⟹ **формула =
критерій, не ширша**: кожен її член реалізується, і жоден реалізовний ambient з-під неї не випадає. ∎

### §7.4 · Таблиця значень

> **Індексована ВХОДОМ формули — диск-ФОРМОЮ, а не типом Браве і не порядком диска.** Таблиця
> значень мусить бути впорядкована тим, від чого значення залежить. Тип — **мітка обраного
> представника**.
> **Група диска входом НЕ є.** Колонка `b̄` стоїть у таблиці саме тому: без неї рядок «`ℤ/6` → 2»
> суперечив би парі §7.2 («`ℤ/6` → 1»), хоча обидва числа правильні — це **дві різні форми на
> одній групі**.

| `disc(L)` — група | `\|disc\|` | `b̄(x,x)` на твірному склейки | **ціна** | свідок `(n, m)` | *мітка представника* |
|:--|--:|:--|--:|:--|:--|
| `0` | 1 | — | **1** | (1, 1) | *cP* |
| `ℤ/2` | 2 | `1/2` | **1** | (2, 2) | *tP* |
| `ℤ/4` | 4 | `3/4` | **3** | (12, 4) | *cF* «FCC» |
| `ℤ/6` | 6 | `1/3` | **2** | (3, 3) | *oP* |
| `ℤ/8` | 8 | `1/2` | **4** | (8, 4) | *tI* |
| `ℤ/3 × ℤ/3` | 9 | `1/3` | **3** | (3, 3) | *hP* |
| `ℤ/4 × ℤ/4` | 16 | `1/2` | **8** | (8, 4) | *cI* «BCC» |
| `ℤ/2 × ℤ/8` | 16 | `3/8` | **6** | (24, 8) | *oC* |
| `ℤ/2 × ℤ/10` | 20 | `2/5` | **8** | (10, 5) | *hR* |
| `ℤ/24` | 24 | `1/8` | **3** | (8, 8) | *oF* |
| `ℤ/2 × ℤ/14` | 28 | `3/14` | **6** | (42, 14) | *mC* |
| `ℤ/51` | 51 | `1/17` | **3** | (17, 17) | *aP* |
| `ℤ/69` | 69 | `2/69` | **2** | (138, 69) | *mP* |
| `ℤ/2 × ℤ/2 × ℤ/24` | 96 | `5/24` | **20** | (120, 24) | *oI* |

> **Як читати колонку `b̄`.** Це значення на твірному **підгрупи склейки** (елемент порядку `m`) —
> саме воно входить у конгруенцію `j·k² ≡ b̄·m (mod m)`. Значення на твірному **всього** диска може
> бути іншим: для рядка *oP* `b̄(g,g) = 5/6` на твірному `ℤ/6`, тоді як у таблиці стоїть `1/3` —
> значення на `x = 2g` порядку 3. **Кожен надрукований свідок перевіряється з рядка:** `j = n/m`,
> `ціна = |disc|·n/m²`; для **нециклічних** форм незалежна перевірка **мінімальності** потребує
> повної диск-форми — supplementary **S2** (`S2_discriminant_form_grams.md`).
> **Дві форми на `ℤ/6`, які цю колонку й виправдовують:** `b̄(g,g) = 5/6` (рядок *oP*) дає ціну 2;
> `b̄(g,g) = 1/6` (пара §7.2) дає ціну 1. Група одна, ціни різні — бо форми різні.

*(Таблиця — значення формули, одним методом; представники валідовані `|Aut|`, 14/14; Ґрами —
supplementary.)* Для **нециклічних** `disc(L)` колонка `b̄` задає значення лише на твірному
склейки, не всю форму: рядок перевіряє **верхню межу** ціни, а мінімальність — за повною
диск-формою; повні Ґрами диск-форм усіх шести нециклічних рядків — supplementary **S2**
(`S2_discriminant_form_grams.md`).

**Видно двоє, що таблиця по типах ховала:**
**(i)** при однаковому порядку форма розрізняє: `ℤ/4×ℤ/4` → 8 проти `ℤ/2×ℤ/8` → 6;
**(ii)** **ціна НЕ монотонна за `|disc|`** — `ℤ/69` → 2, `ℤ/4` → 3, `ℤ/8` → 4: більший диск може
бути **дешевшим**. ⟹ **жодного «розмір веде»**; ціна є функцією диск-форми, і тільки.

---

## §8 · P7.Cor4 — адреси центрувань

> **P7.Cor4.** **FCC — `cost = 3`**, **BCC — `cost = 8`**; обидва недосяжні на унімодулярному
> страті, але з **різними** адресами, і різниця пояснюється **диск-формою**.

### Таблиця 1 — механізм блокування: три рядки, не один

Для FCC `disc = ℤ/4`, `b̄ = 3/4`; допустимі `m ∈ {1, 2, 4}`, і `d = |disc|·j/m` з
`j·k² ≡ b̄·m (mod m)`.

| `d = \|det Λ_W\|` | статус | чим саме заблоковано |
|:--|:--|:--|
| **d = 1** | заблоковано | потрібне `k² ≡ 3 (mod 4)`, а квадрати mod 4 — `{0,1}`: **квадратичний лишок** |
| **d = 2** | заблоковано | обидва маршрути падають на **конгруенції за модулем 2**: `m = 2` дає `1 ≢ 0 (mod 2)`, `m = 4` дає `2k² ≡ 3 (mod 4)` — парне число проти непарного |
| **d = 3** | **не заблоковане нічим** | воно **і є мінімум** — надґратка явна, `\|det\| = 3`, (3,1), `(n,m) = (12,4)` |

> **Про слово «парність» у рядку `d = 2`.** Тут ідеться про **парність цілих чисел** у конгруенції,
> **не** про парність ґратки (тип I/II) з умови **(Т2)**. Омонім названо: після §3
> «парність» у цій роботі — несуче слово, і в Таблиці 1 воно означає **інше**.
> «Блокує **форма**, а не порядок» — висновок із трьох рядків, не замість них.

> **Статус числа `8` для BCC — щоб два рівні не читались як один.** `cost(BCC) = 8` здобуте
> **абстрактно**: формула P7.Thm2(b) рахує ціну з `b̄` самої `L`, а реалізовність кожного члена
> автоматична (Thm2.9). Воно **не залежить** від відкритого пункту §11 — той стосується **повної
> ізометрії** `Γ^⊥/Γ` для BCC, тобто **ідентифікації конкретного ambient**, а не значення ціни.
> Ціна відповідає «наскільки дорого», ідентифікація — «який саме»; друге лишається незакритим.

Це **native-причина** виключення FCC з унімодулярного страта: раніше вона стояла як **спостережена
перешкода**, тепер **виводиться** з критерію.
**Асиметрія `3 < 8`** пояснюється **різними формами** — і **не** монотонністю за розміром (§7.4).

---

## §9 · Виміряне поруч: `cost` і `n_min` — не одна шкала

Чи не є «ціна ambient» іншим виразом для `n_min` (P6)? **Ні:**

```
        Spearman( n_min , cost )  ≈  0,78     на одному наборі представників
        Spearman( n_min , cost )  ≈  0,36     на альтернативному наборі
```

Коефіцієнт опоро-залежний і не цитується як величина.

**Висновок — двома окремими твердженнями, кожне зі своїм свідком** (одне без іншого не
доводиться: два розходження порядку **не** спростовують функціональної залежності — функція
цілком може мати `f(43) = 3`, `f(13) = 8`):

**(i) Порядок не узгоджений (немонотонність):**
```
        triclinic       n_min = 43 ,  cost = 3
        rhombohedral    n_min = 13 ,  cost = 8
```
більший `n_min` — менша ціна; саме це (і лише це) відбиває `Spearman < 1`.

**(ii) `cost` не є функцією `n_min` — колізія:** `n_min` — інваріант **голоедрії** (P6), а ціна —
ні: обидві ґратки пари §7.2(ii) гексагональні, тобто мають **ту саму** голоедрію і `n_min = 6`,
але ціни **1** (`A₂⊕⟨2⟩`) і **3** (рядок *hP*). Однаковий аргумент, різні значення — функції
немає.


---

## §10 · Відношення до попередніх робіт і джерела

| механізм | джерело |
|:--|:--|
| термінологія склейки (glue group / glue vectors); теорія диск-форм **парного** випадку (тотожність `disc(Λ) ≅ Γ^⊥/Γ` для непарних доведена в роботі — **P7.Lem1**) | **V. V. Nikulin**, *Integral symmetric bilinear forms and some of their applications*, Izv. Akad. Nauk SSSR **43** (1979); англ. пер. Math. USSR Izv. **14** (1980) — §1.5, §1.5.1. **J. H. Conway, N. J. A. Sloane**, *Sphere Packings, Lattices and Groups*, 3rd ed., Springer 1999 — **Ch. 4** |
| клас = спінорний рід для **невизначених** форм **рангу ≥ 3** | **M. Eichler**, *Quadratische Formen und orthogonale Gruppen*, Springer 1952. **M. Kneser**, *Klassenzahlen indefiniter quadratischer Formen…*, Arch. Math. **7** (1956). Сучасний виклад: **J. W. S. Cassels**, *Rational Quadratic Forms*, Academic Press 1978 — **Ch. 11**; **O. T. O'Meara**, *Introduction to Quadratic Forms*, Springer — **§102** |
| один спінорний рід у роді при малому `\|det\|`, ранг 4 | **Conway–Sloane**, SPLAG 3rd ed. — **Ch. 15, Cor. 22** |
| коректність числення Конвея–Слоуна для 2-адичних ґраток (перше опубліковане доведення; **§6 — виправлення хибної канонічної форми CS**, слідом за Allcock, Memoirs AMS 220; несе склейку §3.5: Lemma 5.2, Lemma 6.1, Theorem 6.2) | **D. Allcock, I. Gal, A. Mark**, *The Conway–Sloane calculus for 2-adic lattices*, `arXiv:1511.04614` |
| **невизначені** парні унімодулярні (`sig ≡ 0 mod 8`); класифікація непарних | **J.-P. Serre**, *A Course in Arithmetic*, GTM 7 — **Ch. V**. **J. Milnor, D. Husemoller**, *Symmetric Bilinear Forms*, Springer 1973 — **Ch. II §5** |
| формула Мілґрема (`σ` диск-форми `mod 8`); виключення парного конкурента | **Milnor–Husemoller**, *Symmetric Bilinear Forms* — **Appendix**; **SPLAG** — формула **(30)** |
| умови існування p-адичних символів — несучий крок **P7.Prop1** (§3.5); «канонічна форма» §7.6 у кроках **не вживається**: її єдиність у загальному випадку хибна (див. рядок AGM) | **Conway–Sloane**, SPLAG 3rd ed. — **Ch. 15, §7.7** (Theorems 10, 11) |
| символи роду (довідково; існування-теореми Нікуліна §1.9/§1.11 — парний випадок, у кроках роботи **не вживаються**: всі амбієнти й надґратки будуються явно) | **Нікулін** §1.9, §1.11 |
| кристалографічне обмеження в ранзі 3 *(історична довідка)* | класика XIX ст.; сучасні виклади — **W. Scherrer** (1946), **H. S. M. Coxeter**, *Introduction to Geometry* (1969) |

> **СКОУП-РЯДОК, БЕЗ ЯКОГО ПОСИЛАННЯ НА НІКУЛІНА ЧИТАЄТЬСЯ ШИРШЕ, НІЖ ВОНО Є.**
> Теорія диск-форм Нікуліна сформульована для **ПАРНИХ** ґраток. **Наш випадок непарний** — і
> працює **нативно**, бо весь критерій від початку побудований на **білінійній** диск-формі
> `b mod ℤ` (`P7.Def1`), а не на квадратичній `q mod 2ℤ`, визначеній лише для парних.
> ⟹ ми **не** застосовуємо результат Нікуліна поза його скоупом: звідти беруться **термінологія
> склейки й тотожність `disc(Λ) ≅ Γ^⊥/Γ`**, а непарний випадок несе власна побудова. Ціна цього
> вибору — що білінійна форма не бачить парності — сплачена явно: біт стоїть у даних критерію
> **(Т2)**, а не ховається в імпорті.

**Номери теорем наводяться лише там, де звірені проти видання; інакше — глава або розділ.**

**Що в цій роботі власне:**

1. **критерій відносно фіксованого ambient** із `Γ^⊥/Γ ≅ disc(Λ_W)` **як формою** і з **парністю**
   в даних (непарний випадок);
2. **згортання назад**: `disc = 0` відтворює умову `⟨1/n⟩` preprint-6;
3. **ціна як інваріант диск-форми** плюс **замкнена формула** зі скінченним діапазоном — **без
   параметра пошуку**;
4. **адреси центрувань** із розділеним механізмом блокування (Таблиця 1).

---

## §11 · Межі й відкрите

### (а) Відкрите

| питання | стан |
|:--|:--|
| **характеризація (Т0)-ґраток** — для яких `Λ_W` трійка «сигнатура + `b̄` + парність» визначає рід у **загальному** вигляді | відкрите для довільного `det` і рангу ≠ 4; для рангу 4, sig (3,1), `\|det\| ≤ 4` **доведено** переліком родів (P7.Prop1, §3.5) |
| ~~леми «парного конкурента немає»~~ | **закрито** — виписано таблицею §3.4 (`σ mod 8`, усі уточнення `q`, перебір повний) |
| **повна форма `Γ^⊥/Γ` для BCC** | порядок і фактори обчислені; повна ізометрія — не зроблена. **Ціни це не стосується** — `cost(BCC) = 8` абстрактне (§8) |
| **γ-свобода при нециклічному диску** | відкрите **без джерела** |
| **простіший вигляд `j_min(m)`** | можливо, через квадратичні лишки / CRT |
| **ціна як інваріант ТИПУ** | **закрито негативно** — свідок §7.2(ii): той самий тип, різні ціни |

> **Рядок-місток** (бо `A₂⊕⟨2⟩` працює двічі): **там однакова диск-форма — однакова ціна навіть при
> різних родах; тут однаковий ТИП — різна ціна. Саме це й показує, що змінна є диск-форма.**

### (б) Паркування, назване вголос

Вибірка `n ≤ 4` не відтворює навіть **відомий** унімодулярний образ (дає `{tetragonal, cubic}`
замість семи; `triclinic` потребує `n = 43`) ⟹ різниця «по родах» на ній була б **артефактом
обрізання**. Потрібен Fincke–Pohst до `n = 43` із бюджетом.

### (в) Про метод: чому в теоремі немає межі пошуку

**Обмежений пошук (межа `D_max = 40`) дав завищені значення ціни — тричі.** Причина щоразу та
сама: для великих дискримінантів оптимальне `m` велике, `n = d·m²/|disc|` вискакує за межу,
рядок **випадає з перебору**. *(Позначення: `D_max` — межа обрізання старого пошуку; з множиною
дільників `D(L)` у P7.Def6 спільного нічого.)*

**Крок скінченності (Thm2.3–Thm2.6) існує саме для того, щоб межі пошуку в теоремі не було
взагалі.** `m | exp(disc L)` — умова **структурна**, не обчислювальна.
**Висновок методу: названа структура не перевіряється скануванням** — якщо задача скінченна за
структурою, це треба **використати**, а не апроксимувати межею.

### (г) Що не є нашим результатом

**Вікно (квазіперіодика) — окремий вхід, не наслідок критерію.**
Скінченнопорядкові цілі ізометрії `I_{3,1}` мають порядки з `{1,2,3,4,6}`: осі порядків **5, 8, 12**
не зберігають (3,1) — **виміряно**; порядок **10** виключається як наслідок (квадрат має порядок 5)
— **виведено**. Детальний розгляд не входить до цієї роботи.

> Це твердження про **скоуп**, не обіцянка: сказано, **чого немає**, без «пізніше ми покажемо».

